<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no">
    <meta name="turbo-cache-control" content="no-cache">
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
    <meta name="renderer" content="webkit">
    <meta http-equiv="cleartype" content="on">
    <meta name="HandheldFriendly" content="True">
    <meta name="MobileOptimized" content="320">
    <% site_name = current_organization&.name || Rails.application.config.x.appname %>
    <title>
    <%- if content_for?(:title) %>
      <%= yield(:title) + ' | ' + site_name %>
    <%- else %>
      <%= site_name %>
    <%- end %>
    </title>
    <%= csrf_meta_tags %>
    <%= action_cable_meta_tag %>
    <%= content_for?(:head) ? yield(:head) : '' %>
    <%= stylesheet_link_tag 'application', media: 'all' %>
    <!-- Load error handler early to catch all errors -->
    <%= javascript_include_tag 'error_handler' if Rails.env.development? %>
    <%= javascript_include_tag 'application', type: "module" %>
    <!-- vConsole for mobile debugging (add ?vconsole=1 to URL to enable) -->
    <% if params[:vconsole] == '1' %>
      <script src="https://unpkg.com/vconsole@latest/dist/vconsole.min.js"></script>
      <script>
        var vConsole = new window.VConsole();
        console.log('vConsole loaded - check bottom-right corner for debug panel');
      </script>
    <% end %>
    <!-- Use organization logo as favicon if available, otherwise use default -->
    <% if current_organization&.logo&.attached? %>
      <%= favicon_link_tag url_for(current_organization.logo) unless Rails.env.test? %>
    <% else %>
      <%= favicon_link_tag asset_path("app-favicon.svg") unless Rails.env.test? %>
    <% end %>
  </head>
  <body class="flex flex-col min-h-screen text-foreground <%= body_class %>">
    <main class="flex-1 flex flex-col bg-surface">
      <%= render 'shared/navbar' unless @full_render %>
      <%= render 'shared/flash_messages' %>
      <%= yield %>
    </main>
    <% if Rails.env.development? %>
      <div id="system-monitor-stream" data-turbo-permanent>
        <%= turbo_stream_from "system_monitor" %>
      </div>
    <% end %>
  </body>
</html>
